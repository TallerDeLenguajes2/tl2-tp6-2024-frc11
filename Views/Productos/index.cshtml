@model List<Productos>

<h1>Lista de Productos</h1>

<table class="table table-bordered">
    <thead>
        <tr>
            <th>ID</th>
            <th>Nombre</th>
            <th>Precio</th>
            <th>Acción</th>
        </tr>
    </thead>
    <tbody>
        @foreach(var producto in Model){
            <tr>
                <td>@producto.IdProducto</td>
                <td>@producto.Descripcion</td>
                <td>$@String.Format("{0:N0}", producto.Precio)</td>
                <td>
                    <a asp-controller="Productos" asp-action="ModificarProducto" asp-route-id="@producto.IdProducto" class="btn btn-secondary"> Modificar</a>
                    <a asp-controller="Productos" asp-action="EliminarProducto" asp-route-id="@producto.IdProducto" class="btn btn-danger"> Borrar</a>
                </td>
            </tr>
        }
    </tbody>
</table>
<div class="text-center">
    <a asp-controller="Productos" asp-action="AltaProducto" class="btn btn-primary">Agregar Producto</a>
</div>